/*Ingresar N artículos (códigos) y sus correspondientes precios.
Indicar el código del artículo más caro y el precio del más barato. NOTA: todos los artículos tienen precios distintos.*/

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i, n,codigo,precio;
    printf("Ingerese la cantidad de articulos: ");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        printf("Ingrese el codigo del articulo: ");
        scanf("%d",&codigo);
        printf()
    }
    return 0;
}
